import { expect } from "chai";
import { getAllSpells } from "../src/services/spell.service";
before("All", async function () {});
describe("Spell Service", function () {
  it("Should Run", function () {
    expect(true).to.equal(true);
  });
});
